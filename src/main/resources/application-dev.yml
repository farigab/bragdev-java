# Configuração de Desenvolvimento
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    bragdoc: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG

server:
  port: 8080

app:
  cookie:
    domain: localhost
    secure: false
    same-site: Lax
